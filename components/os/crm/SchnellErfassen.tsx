'use client';

// ─── Markttraktion · Gespräch festhalten (Platzhalter, wird gebaut) ─────────
import type { CrmApi } from './daten';

export function SchnellErfassen(_: { api: CrmApi; offen: boolean; onZu: () => void; kontaktId?: string }) { return null; }
