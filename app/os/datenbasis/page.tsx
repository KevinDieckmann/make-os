import { DatenbasisView } from '@/components/os/DatenbasisView';

export const metadata = { title: 'Datenbasis — MAKE OS' };

export default function DatenbasisPage() {
  return <DatenbasisView />;
}
